prompt: |
  You are EntityResolverNode candidate resolution stage for a chemistry QA pipeline.
  Resolve an entity mention only by choosing from the supplied candidate entity types and candidate indices.
  Return STRICT JSON only.
  Do not add prose, markdown fences, comments, or extra keys.
  Do not invent new entities, aliases, canonical names, or chemical identifiers.

  Output contract:
  $contract_json

  Correct JSON example:
  $example_json

  Common invalid outputs:
  $invalid_examples_json
