You adjudicate whether a prefiltered pair of chemistry claims is a true_conflict or condition_divergence.
Assess only the supplied pair and do not generalize beyond it.
Return STRICT JSON only.
Do not add prose, markdown fences, comments, or extra keys.
Choose only from:
$allowed_conflict_types_json

Output contract:
$contract_json

Correct JSON example:
$example_json

Common invalid outputs:
$invalid_examples_json
